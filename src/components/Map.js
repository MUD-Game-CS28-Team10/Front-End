import React from 'react';

const Map = (props) => {

  const { currentRoomNum, currentX, currentY } = props;

  return (
    <div className="map">
      <h1>Game Map</h1>
    </div>
  );
};

export default Map;

// const productionURL = `https://;
const testingURL = `https://lambda-mud-test.herokuapp.com/api`;
const localhost = `http://localhost:8000/api`;

const API_URL = testingURL;

export default API_URL;